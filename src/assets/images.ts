// Run this in your terminal to get the base64 strings:
// node -e "const fs=require('fs'); console.log(fs.readFileSync('./tmp/logo.jpeg').toString('base64'))"
// node -e "const fs=require('fs'); console.log(fs.readFileSync('./tmp/stamp.jpg').toString('base64'))"
// Then paste the output below

export const LOGO_B64 = "PASTE_FULL_BASE64_STRING_OF_logo.jpeg_HERE";

export const STAMP_B64 = "PASTE_FULL_BASE64_STRING_OF_stamp.jpg_HERE";